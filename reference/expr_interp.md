# Process unquote operators in a captured expression

**\[deprecated\]** `expr_interp()` is deprecated, please use
[`inject()`](https://rlang.r-lib.org/reference/inject.md) instead.

## Usage

``` r
expr_interp(x, env = NULL)
```

## Arguments

- x, env:

  **\[deprecated\]**

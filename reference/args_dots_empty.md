# Helper for consistent documentation of empty dots

Use `@inheritParams rlang::args_dots_empty` in your package to
consistently document `...` that must be empty.

## Arguments

- ...:

  These dots are for future extensions and must be empty.

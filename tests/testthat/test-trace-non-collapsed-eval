Full:
█
├─eval()
└─.handleSimpleError(...)
  └─h(simpleError(msg, call))

Collapsed:
█
├─[ eval() ]
└─.handleSimpleError(...)
  └─h(simpleError(msg, call))

Branch:
 ─eval()
 ─.handleSimpleError(...)
 ─h(simpleError(msg, call))

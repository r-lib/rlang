Full:
█
├─base::eval()
└─base::.handleSimpleError(...)
  └─h(simpleError(msg, call))

Collapsed:
█
├─[ base::eval() ]
└─base::.handleSimpleError(...)
  └─h(simpleError(msg, call))

Branch:
 ─base::eval()
 ─base::.handleSimpleError(...)
 ─h(simpleError(msg, call))

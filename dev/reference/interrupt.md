# Simulate interrupt condition

`interrupt()` simulates a user interrupt of the kind that is signalled
with `Ctrl-C`. It is currently not possible to create custom interrupt
condition objects.

## Usage

``` r
interrupt()
```

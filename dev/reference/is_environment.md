# Is object an environment?

`is_bare_environment()` tests whether `x` is an environment without a s3
or s4 class.

## Usage

``` r
is_environment(x)

is_bare_environment(x)
```

## Arguments

- x:

  object to test
